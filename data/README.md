# Data Folder Policy

This project ignores `/data/*` by default to avoid committing private or heavy datasets.

Tracked files are allowed only in:

- `data/examples/` for tiny demo documents.

If you need custom local data, put it in `/data/` but outside `data/examples/`.
