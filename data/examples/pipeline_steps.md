A typical RAG pipeline has four stages: ingest, index, retrieve, generate.
Ingest loads raw documents.
Index converts chunks into embeddings and stores them.
Retrieve finds relevant chunks for a query before generation.
