Transformers replaced many recurrent models in NLP.
Self-attention lets each token attend to every other token.
RNNs process tokens sequentially, while transformers process in parallel.
Parallel processing usually improves training throughput.
